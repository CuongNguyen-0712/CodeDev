'use client'

export default function Settings() {
    return (
        <main id="main">
            <h1>Hello</h1>
            <button onClick={() => router.push('/main')}>Back</button>
        </main>
    )
}