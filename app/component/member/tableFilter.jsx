import { FaCaretUp } from "react-icons/fa";

export default function TableFilter({filter}) {
    return(
        <div className={`table-filter ${filter ? 'active' : ''}`}>
            
        </div>
    )
}